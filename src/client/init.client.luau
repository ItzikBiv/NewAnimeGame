--!strict
--[[
	Client bootstrap.

	Placeholder entry point so `rojo build` succeeds against default.project.json.
	HUD, roll machine UI, inventory and grid placement land here in Epic 1.
]]

print("[RollAnimeToFight] client booted")
